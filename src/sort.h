#ifndef SORT_H
#define SORT_H

#include <stdio.h>
#include "text.h"
#include "UI.h"

// Sorts strings with given parameters.
void sort_strings(text_t *text, const params_t *params);

#endif // SORT_H

